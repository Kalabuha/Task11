<Window x:Class="Bank_StashYourCrap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Bank_StashYourCrap.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindiwViewModel/>
    </Window.DataContext>
    
    <DockPanel>
        <!--Menu-->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="{Binding Localization.StringLibrary[1]}">
                <MenuItem Header="{Binding Localization.StringLibrary[2]}"/>
                <Separator/>
                <MenuItem Header="{Binding Localization.StringLibrary[3]}"/>
            </MenuItem>
            <MenuItem Header="{Binding Localization.StringLibrary[4]}">
                <MenuItem Header="{Binding Localization.StringLibrary[5]}"/>
                <Separator/>
                <MenuItem Header="{Binding Localization.StringLibrary[6]}"/>
            </MenuItem>
        </Menu>

        <!--Statusbar-->
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding Status}"/>
        </StatusBar>

        <!--Data display-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <!--Search controllers-->
                <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Localization.StringLibrary[7]}"/>
                <Button Grid.Row="0" Grid.Column="2" Content="{Binding Localization.StringLibrary[8]}"/>

                <!--List of clients-->
                <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
                    <ListBox BorderBrush="DarkGray" BorderThickness="2" ItemsSource="{Binding Clients}" DisplayMemberPath="Name">

                    </ListBox>
                </ScrollViewer>

                <!--Buttons CRUD-->
                <Button Grid.Column="0" Grid.Row="2" Content="{Binding Localization.StringLibrary[9]}"/>
                <Button Grid.Column="1" Grid.Row="2" Content="{Binding Localization.StringLibrary[10]}"/>
                <Button Grid.Column="2" Grid.Row="2" Content="{Binding Localization.StringLibrary[11]}"/>
            </Grid>

            <!--Data of client-->
            <ListView Grid.Column="1"  BorderBrush="DarkGray" BorderThickness="2">

            </ListView>
        </Grid>
    </DockPanel>
</Window>
